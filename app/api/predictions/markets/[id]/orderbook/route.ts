import { getPredictionBook } from "@/src/polymarket/client";
export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> }) { const tokenId = (await params).id; if (!/^\d{20,90}$/.test(tokenId)) return Response.json({ error: "Invalid token" }, { status: 400 }); try { return Response.json({ book: await getPredictionBook(tokenId), source: "Polymarket CLOB", receivedAt: new Date().toISOString() }, { headers: { "cache-control": "no-store" } }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Order book unavailable" }, { status: 503 }); } }
