import { getPredictionMarket, getPredictionStats } from "@/src/polymarket/client";
import { persistPredictionStats } from "@/src/polymarket/repository";
export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> }) { try { const market = await getPredictionMarket((await params).id); if (!market) return Response.json({ error: "Market not found" }, { status: 404 }); const stats = await getPredictionStats(market); const persistent = await persistPredictionStats(stats).catch(() => false); return Response.json({ stats, persistent, source: "Polymarket Gamma + Data APIs", receivedAt: new Date().toISOString() }, { headers: { "cache-control": "public, max-age=30" } }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Stats unavailable" }, { status: 503 }); } }
