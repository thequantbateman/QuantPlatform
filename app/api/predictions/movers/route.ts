import { discoverPredictionEvents } from "@/src/polymarket/client";
export async function GET() { try { const discovery = await discoverPredictionEvents({ limit: 100, order: "volume24hr" }); const movers = [...discovery.markets].filter((market) => market.change1d !== null).sort((left, right) => Math.abs(right.change1d ?? 0) - Math.abs(left.change1d ?? 0)).slice(0, 25); return Response.json({ movers, receivedAt: discovery.receivedAt, source: discovery.source }, { headers: { "cache-control": "public, max-age=45" } }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Movers unavailable" }, { status: 503 }); } }
