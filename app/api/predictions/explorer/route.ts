import { predictionCoverage, recentPersistedQuotes } from "@/src/polymarket/repository";
export async function GET(request: Request) { const limit = Math.min(500, Math.max(1, Number(new URL(request.url).searchParams.get("limit") ?? 100) || 100)); try { const [coverage, quotes] = await Promise.all([predictionCoverage(), recentPersistedQuotes(limit)]); return Response.json({ coverage, quotes, receivedAt: new Date().toISOString() }, { headers: { "cache-control": "no-store" } }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Explorer unavailable", coverage: { persistent: false }, quotes: [] }, { status: 503 }); } }
