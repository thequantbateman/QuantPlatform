import { predictionCoverage, recentPersistedQuotes } from "@/src/polymarket/repository";
import { reportServerError } from "@/src/server/observability";
export async function GET(request: Request) { const limit = Math.min(500, Math.max(1, Number(new URL(request.url).searchParams.get("limit") ?? 100) || 100)); try { const [coverage, quotes] = await Promise.all([predictionCoverage(), recentPersistedQuotes(limit)]); return Response.json({ coverage, quotes, receivedAt: new Date().toISOString() }, { headers: { "cache-control": "no-store" } }); } catch (error) { reportServerError("prediction-explorer", error); return Response.json({ error: "Explorer unavailable", coverage: { persistent: false }, quotes: [] }, { status: 503 }); } }
