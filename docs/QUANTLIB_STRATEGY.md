# QuantLib strategy

`OUR IMPLEMENTATION → educational, readable, testable → validated against QuantLib`.

V0 does not import QuantLib. An optional validation environment may construct matching European instruments and compare PV/Greeks. QuantLib becomes a production engine only behind a provider contract when calendars, schedules, curves and complex instruments exceed the transparent engine's intended scope.
