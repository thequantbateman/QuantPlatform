# Voice and avatar roadmap

Future flow: browser microphone → Realtime AI session → quant agent/tools → streamed audio response → avatar phoneme/state animation. Microphone permission is explicit and optional. Raw audio is not retained by default; transcript and retention settings must be visible. The text tutor and local fallback remain functional when voice is unavailable.
