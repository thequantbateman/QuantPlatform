"""Optional FastAPI entrypoint sketch; dependencies are intentionally not installed."""

# from fastapi import FastAPI
# app = FastAPI(title="TheQuantBateman Quant Service", version="0.1.0")
#
# @app.get("/health")
# def health() -> dict[str, str]:
#     return {"status": "ok"}
